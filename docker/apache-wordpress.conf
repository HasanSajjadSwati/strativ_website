# Allow WordPress .htaccess (pretty permalinks) to take effect.
# Debian's default apache2.conf sets AllowOverride None for /var/www/,
# which makes every non-root URL 404. Override it for the web root.
<Directory /var/www/html>
    Options FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
